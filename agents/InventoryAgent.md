# InventoryAgent

Input: a public GitHub repository name. Output: its default-branch package manifests and ecosystem/package mappings. Coverage records Git-tree truncation and manifest caps. Runtime: `oss_timeline/core.py`.
