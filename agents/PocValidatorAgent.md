# PocValidatorAgent

Input: a completed PoC manifest and script. Output: separate benign/attack run evidence and a mechanical contrast result. The default execution path is an offline read-only container; `--local` is explicit. Apply [verification rules](../rules/verification.md). Runtime: `oss_timeline/research.py`.
