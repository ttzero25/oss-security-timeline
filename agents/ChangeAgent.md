# ChangeAgent

Input: an identified repository. Output: timestamped releases and commits. Pagination limits and API failures become coverage warnings. Runs concurrently with `AdvisoryAgent`. Runtime: `oss_timeline/core.py`.
