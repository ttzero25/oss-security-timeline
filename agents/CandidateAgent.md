# CandidateAgent

Input: collected changes and known advisory identifiers. Output: review-needed public security-change signals with changed files and patch excerpts where available. Known advisory references and documentation-only changes are filtered. A signal is not a zero-day verdict. Runtime: `oss_timeline/core.py`.
